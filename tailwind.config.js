// tailwind.config.js

/** @type {import('tailwindcss').Config} */
const tailwindConfig = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
    // Agrega personalizaciones del tema aquí si es necesario
  },
  plugins: [
    // Agrega plugins de Tailwind aquí si es necesario
  ],
};

export default tailwindConfig;
